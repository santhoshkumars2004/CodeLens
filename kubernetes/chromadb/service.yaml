apiVersion: v1
kind: Service
metadata:
  name: chromadb
  namespace: codelens
spec:
  selector:
    app: codelens
    component: chromadb
  ports:
    - port: 8000
      targetPort: 8000
  clusterIP: None
