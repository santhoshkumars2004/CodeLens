apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: codelens
spec:
  replicas: 2
  selector:
    matchLabels:
      app: codelens
      component: frontend
  template:
    metadata:
      labels:
        app: codelens
        component: frontend
    spec:
      containers:
        - name: frontend
          image: codelens/frontend:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          env:
            - name: NEXT_PUBLIC_API_URL
              value: "http://backend:8000"
