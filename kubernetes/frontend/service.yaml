apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: stacksense
spec:
  selector:
    app: stacksense
    component: frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: LoadBalancer
